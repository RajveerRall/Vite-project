I can see the issue! You've made inconsistent changes to the file. Looking at your edits:

1. **In the prefetch function**: You're using `params.set('rate', speedParam)` with the `%` symbol
2. **In the playChunk function**: You're using `params.append('rate', speedPercent.toString())` without the `%` symbol

This inconsistency is causing the problem. Both functions need to use the same format.

## The Fix

You need to make both functions consistent. Here's the corrected version:

```typescript:src/hooks/useReaderTTS.ts
// ... existing code ...

  // === Prefetch chunks function ===
  const prefetchChunks = useCallback(async (startIndex: number) => {
    // ... existing code ...
    
    try {
      const ttsApiUrl = import.meta.env.VITE_TTS_API_URL || '';
      const textChunk = chunksToFetch[i];
      const apiUrl = ttsApiUrl ? `${ttsApiUrl}/api/tts` : '/api/tts';
      
      // Build query parameters with voice and speed
      const params = new URLSearchParams({
        text: textChunk,
        voice: selectedVoice,
        format: 'audio-24khz-48kbitrate-mono-mp3'
      });
      
      // Add speed parameter if not 1 (normal speed)
      if (ttsSpeed !== 1) {
        const speedPercent = Math.round((ttsSpeed - 1) * 100);
        const speedParam = speedPercent > 0 ? `+${speedPercent}%` : `${speedPercent}%`;
        // Fix: Use set() instead of append() to avoid double encoding
        params.set('rate', speedParam);
      }
      
      let response = await fetch(`${apiUrl}?${params.toString()}`);
      // ... rest of existing code ...
    } catch (error) {
      // ... existing error handling ...
    }
  }, [chunks, currentChunkIndex, selectedVoice, ttsSpeed]);

  // === Play chunk function ===
  const playChunk = useCallback(async (index: number) => {
    // ... existing code until the fetch part ...
    
    if (!audioBuffer.current[index]) {
      console.log(`[playChunk] Playing chunk #${index} from NETWORK.`);
      try {
        const textChunk = chunks[index];
        
        // Build query parameters with voice and speed
        const params = new URLSearchParams({
          text: textChunk,
          voice: selectedVoice,
          format: 'audio-24khz-48kbitrate-mono-mp3'
        });
        
        // Add speed parameter if not 1 (normal speed)
        if (ttsSpeed !== 1) {
          const speedPercent = Math.round((ttsSpeed - 1) * 100);
          const speedParam = speedPercent > 0 ? `+${speedPercent}%` : `${speedPercent}%`;
          // Fix: Use set() instead of append() to avoid double encoding
          params.set('rate', speedParam);
        }
        
        let response = await fetch(`/api/tts?${params.toString()}`);
        // ... rest of existing code ...
      } catch (error) {
        // ... existing error handling ...
      }
    }
  }, [chunks, clearResumeIndex, prefetchChunks, readerInstanceId, selectedVoice, ttsSpeed]);

// ... rest of existing code ...
```

## Key Points:

1. **Both functions must use the same format**: `params.set('rate', speedParam)` with the `%` symbol
2. **Use `params.set()` not `params.append()`**: This prevents duplicate parameters and encoding issues
3. **Always include the `%` symbol**: This matches what your Python server expects

## What You Need to Do:

1. **Fix the playChunk function** to match the prefetch function
2. **Use `params.set('rate', speedParam)`** in both places
3. **Include the `%` symbol** in both places
4. **Rebuild and redeploy** your application

The inconsistency between the two functions is why you're seeing both `rate=-20%25` and `rate=-20` in your logs.